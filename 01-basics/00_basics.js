// interpreted lang
// weakly type --- data type of a variable can be change
// dynamically type
// single threaded---a programming language that can only execute one task at a time, in sequence, with each task completing before the next one starts. Single-threaded languages have a single call stack and a single memory heap. 

// synchronsed --- thts is the lines of codes executes as per their order of writting